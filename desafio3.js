/*
  Inclua
  o campo `avaliacao`
  do tipo `NumberInt`
  e com o valor `0`
  em todos os documentos da coleção.
*/

/*
use commerce;
db.produtos.updateMany({}, { $set: { 'avaliacao': NumberInt('0') } })
*/
