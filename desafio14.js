/*
  Incremente as vendas de todos
  os sanduíches de carne do tipo `bovino` e `pão`
  aos sábados em `120`.
*/

/*
  use commerce;
  db.produtos.updateMany({ 'tags': { $all: ['bovino', 'pão'] } }, { $inc: { 'vendasPorDia.6': 120 } });
*/
