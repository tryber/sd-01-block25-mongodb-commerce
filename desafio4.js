/*
  Incremente
  o valor do campo `avaliacao` em `5`
  em todos os sanduíches de carne do tipo `bovino`.
  Dica: utilize como filtro o campo `tags`.
*/

/*
  use commerce;
  db.produtos.updateMany({ 'tags': 'bovino' }, { $inc: { 'avaliacao': 5 } });
*/
